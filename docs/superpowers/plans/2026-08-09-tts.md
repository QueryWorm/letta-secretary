# TTS (text-to-speech) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить озвучку ответов Маши по триггер-словам («скажи», «расскажи», «озвучь», «прочитай», «голосом», «voice») через edge-tts sidecar + custom tool + persona rule.

**Architecture:** Python sidecar (FastAPI + edge-tts) синтезирует речь, возвращает base64 .ogg + timing. Custom tool агента `speak_text` вызывает sidecar. Persona правило: триггер-слова → только голос; иначе текст + debug `[llm=X.Xs]`.

**Tech Stack:** Python 3.12 (sidecar), FastAPI, edge-tts, ffmpeg, letta-server REST API (tools + persona).

## Global Constraints

- Sidecar endpoint: `http://tts:8000` (внутри docker network)
- Голос по дефолту: `ru-RU-SvetlanaNeural` (Microsoft Svetlana, женский)
- Audio format: OGG/Opus, mono, 16kHz
- Max text: 5000 chars (обрезаем с warning)
- Tool `speak_text` без approval
- Persona правило trigger-слов: «скажи», «расскажи», «озвучь», «прочитай», «голосом», «voice» (без учёта регистра)
- Кеш в sidecar: in-memory LRU 50 entries
- Timing: sidecar возвращает `synthesis_ms` (monotonic); tool возвращает `tool_total_ms`
- No keys in code — edge-tts не требует API ключа
- `TTS_BASE_URL=http://tts:8000` доступен в env `letta-server` через `docker-compose.yml`

## File Structure

| File | Responsibility |
|---|---|
| `tts/requirements.txt` | Python deps |
| `tts/app.py` | FastAPI service: /synthesize, /health, /voices |
| `tts/Dockerfile` | python:3.12-slim + ffmpeg + deps |
| `tts/tests/test_app.py` | pytest tests for sidecar |
| `docker-compose.yml` | Modify: добавить сервис `tts` |
| Persona block `core-memory/blocks/persona` | Modified via API |
| Agent tool `speak_text` | Created via API |
| `docs/superpowers/specs/2026-08-09-tts.md` | Spec (already exists, commit 5b0c934) |

---

### Task 1: TTS sidecar (FastAPI + edge-tts)

**Files:**
- Create: `/home/katya/letta/tts/requirements.txt`
- Create: `/home/katya/letta/tts/app.py`
- Create: `/home/katya/letta/tts/Dockerfile`
- Create: `/home/katya/letta/tts/tests/test_app.py`

**Interfaces:**
- Produces: sidecar at `http://tts:8000` with endpoints
  - `POST /synthesize` body `{text, voice?}` → `{audio_base64, filename, duration_sec, synthesis_ms, truncated?}`
  - `GET /health` → `{status: "ok"}`
  - `GET /voices` → list of voice names

- [ ] **Step 1: Write `tts/requirements.txt`**

```txt
edge-tts>=6.1.9
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pytest>=8.0
httpx>=0.27
```

- [ ] **Step 2: Write `tts/app.py`**

```python
"""TTS sidecar: edge-tts → OGG/Opus audio, with timing."""
import asyncio
import base64
import io
import time
from collections import OrderedDict
from typing import Optional

import edge_tts
from fastapi import FastAPI, HTTPException
from fastapi.responses import JSONResponse
from pydantic import BaseModel, Field

app = FastAPI(title="TTS Sidecar", version="0.1.0")

MAX_TEXT_LEN = 5000
DEFAULT_VOICE = "ru-RU-SvetlanaNeural"
CACHE_SIZE = 50


class SynthesizeRequest(BaseModel):
    text: str = Field(..., min_length=1, max_length=MAX_TEXT_LEN * 2)
    voice: Optional[str] = DEFAULT_VOICE


class SynthesizeResponse(BaseModel):
    audio_base64: str
    filename: str
    duration_sec: float
    synthesis_ms: int
    truncated: bool = False
    text_length: int


_cache: "OrderedDict[str, SynthesizeResponse]" = OrderedDict()


def _cache_get(key: str) -> Optional[SynthesizeResponse]:
    if key in _cache:
        _cache.move_to_end(key)
        return _cache[key]
    return None


def _cache_put(key: str, value: SynthesizeResponse) -> None:
    _cache[key] = value
    if len(_cache) > CACHE_SIZE:
        _cache.popitem(last=False)


async def _synthesize_one(text: str, voice: str) -> bytes:
    communicate = edge_tts.Communicate(text, voice)
    buf = io.BytesIO()
    async for chunk in communicate.stream():
        if chunk["type"] == "audio":
            buf.write(chunk["data"])
    return buf.getvalue()


def _mp3_duration_estimate(mp3_bytes: bytes) -> float:
    """Rough estimate: 16kbps mono for OGG/Opus ~2KB/s. Fallback to byte count."""
    if not mp3_bytes:
        return 0.0
    return round(len(mp3_bytes) / 2000.0, 2)


@app.get("/health")
def health() -> dict:
    return {"status": "ok"}


@app.get("/voices")
def voices() -> dict:
    """Returns short list of common ru-RU voices for testing."""
    return {
        "voices": [
            "ru-RU-SvetlanaNeural",
            "ru-RU-DariyaNeural",
            "en-US-AriaNeural",
        ]
    }


@app.post("/synthesize", response_model=SynthesizeResponse)
async def synthesize(req: SynthesizeRequest) -> SynthesizeResponse:
    t_start = time.monotonic()
    truncated = False
    text = req.text
    if len(text) > MAX_TEXT_LEN:
        text = text[:MAX_TEXT_LEN]
        truncated = True
    cache_key = f"{req.voice}:{text}"
    cached = _cache_get(cache_key)
    if cached is not None:
        return cached
    try:
        mp3_bytes = await _synthesize_one(text, req.voice)
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"edge-tts synthesis failed: {e}")
    if not mp3_bytes:
        raise HTTPException(status_code=500, detail="edge-tts returned empty audio")
    t_end = time.monotonic()
    synthesis_ms = int((t_end - t_start) * 1000)
    audio_b64 = base64.b64encode(mp3_bytes).decode("ascii")
    resp = SynthesizeResponse(
        audio_base64=audio_b64,
        filename="response.ogg",
        duration_sec=_mp3_duration_estimate(mp3_bytes),
        synthesis_ms=synthesis_ms,
        truncated=truncated,
        text_length=len(req.text),
    )
    _cache_put(cache_key, resp)
    return resp
```

- [ ] **Step 3: Write `tts/Dockerfile`**

```dockerfile
FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
        ffmpeg \
        curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app.py .

EXPOSE 8000

HEALTHCHECK --interval=30s --timeout=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 4: Write failing tests in `tts/tests/test_app.py`**

```python
import base64
import pytest
from fastapi.testclient import TestClient

from app import app


@pytest.fixture
def client():
    return TestClient(app)


def test_health(client):
    r = client.get("/health")
    assert r.status_code == 200
    assert r.json() == {"status": "ok"}


def test_voices(client):
    r = client.get("/voices")
    assert r.status_code == 200
    assert "ru-RU-SvetlanaNeural" in r.json()["voices"]


def test_synthesize_basic(client):
    r = client.post("/synthesize", json={"text": "Привет, мир."})
    assert r.status_code == 200
    body = r.json()
    assert "audio_base64" in body
    assert body["filename"] == "response.ogg"
    assert body["duration_sec"] > 0
    assert body["synthesis_ms"] > 0
    assert body["truncated"] is False
    audio = base64.b64decode(body["audio_base64"])
    assert len(audio) > 100


def test_synthesize_with_explicit_voice(client):
    r = client.post("/synthesize", json={"text": "Тест", "voice": "ru-RU-DariyaNeural"})
    assert r.status_code == 200


def test_synthesize_truncation(client):
    long_text = "а" * 6000
    r = client.post("/synthesize", json={"text": long_text})
    assert r.status_code == 200
    assert r.json()["truncated"] is True
    assert r.json()["text_length"] == 6000


def test_synthesize_empty_text_rejected(client):
    r = client.post("/synthesize", json={"text": ""})
    assert r.status_code in (400, 422)


def test_synthesize_cache_hit(client):
    payload = {"text": "Кеш тест.", "voice": "ru-RU-SvetlanaNeural"}
    r1 = client.post("/synthesize", json=payload)
    r2 = client.post("/synthesize", json=payload)
    assert r1.status_code == 200 and r2.status_code == 200
    # Same audio (cache hit)
    assert r1.json()["audio_base64"] == r2.json()["audio_base64"]
    # Cached call still returns same synthesis_ms value (cached)
    assert r1.json()["synthesis_ms"] == r2.json()["synthesis_ms"]
```

- [ ] **Step 5: Build and run container, run tests**

```bash
cd /home/katya/letta
docker compose -f docker-compose.yml -f <(echo "version: '3.8'; services: tts-build: { build: ./tts }") up -d tts-build 2>&1 || \
docker buildx build --load -t letta-tts:test ./tts 2>&1 | tail -5
docker run --rm -d --name letta-tts-test -p 8000:8000 letta-tts:test
sleep 4
# Run tests against the running container
docker exec letta-tts-test pip install pytest httpx 2>&1 | tail -2
docker exec -e TTS_URL=http://localhost:8000 letta-tts-test python3 -m pytest /app/tests/test_app.py -v 2>&1 | tail -25
# Alternative if tests live in /app/tests (need to be in image):
docker exec letta-tts-test bash -c "cd /app && pip install -q pytest httpx && python3 -m pytest tests/test_app.py -v" 2>&1 | tail -20
# Cleanup
docker rm -f letta-tts-test
```

Expected: 7 tests pass. Note: tests will actually hit edge-tts API (network required). If no network, tests fail with HTTP 500 from synthesize — handle in fix round.

- [ ] **Step 6: Manual smoke test of container**

```bash
docker build -t letta-tts:dev ./tts 2>&1 | tail -3
docker run --rm -d --name tts-smoke -p 8000:8000 letta-tts:dev
sleep 3
curl -sS http://localhost:8000/health
echo
curl -sS -X POST http://localhost:8000/synthesize -H "Content-Type: application/json" -d '{"text":"Привет, это тест.","voice":"ru-RU-SvetlanaNeural"}' | python3 -c "
import json,sys,base64
d=json.load(sys.stdin)
print('audio bytes:', len(base64.b64decode(d['audio_base64'])))
print('duration_sec:', d['duration_sec'])
print('synthesis_ms:', d['synthesis_ms'])
"
docker rm -f tts-smoke
```

Expected: audio bytes > 100, duration_sec > 0, synthesis_ms > 0.

- [ ] **Step 7: Commit**

```bash
cd /home/katya/letta
git add tts/requirements.txt tts/app.py tts/Dockerfile tts/tests/test_app.py
git commit -m "feat(tts): edge-tts sidecar (FastAPI) with /synthesize, /health, /voices"
```

---

### Task 2: docker-compose + tool + persona + e2e

**Files:**
- Modify: `/home/katya/letta/docker-compose.yml` (add tts service + env)
- Create: tool `speak_text` via API (runtime, not git)
- Modify: persona via API (runtime, not git)
- Create: `/home/katya/letta/tts/tests/test_e2e.sh` (shell smoke)

**Interfaces:**
- Tool JSON schema (from spec):
  ```json
  {
    "name": "speak_text",
    "description": "Озвучить текст через TTS (edge-tts). Используй когда Игорь просит голосом: слова 'скажи', 'расскажи', 'озвучь', 'прочитай', 'голосом', 'voice'. Возвращает аудио-файл (.ogg), который будет отправлен как голосовое сообщение. БЕЗ отправки текста в этом случае.",
    "parameters": {
      "type": "object",
      "properties": {
        "text": {"type": "string", "description": "Текст для озвучки"},
        "voice": {"type": "string", "default": "ru-RU-SvetlanaNeural", "description": "Голос TTS"}
      },
      "required": ["text"]
    }
  }
  ```
- Tool source code: see below (in Step 2)
- Persona rules (new addition, see Step 3)

- [ ] **Step 1: Add tts service to `docker-compose.yml`**

In `/home/katya/letta/docker-compose.yml`, add a new service **before** the `volumes:` block:

```yaml
  tts:
    build: ./tts
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
```

Then in `letta-server` service, add env `TTS_BASE_URL`:

```yaml
      LITELLM_LOG: INFO
      TTS_BASE_URL: http://tts:8000
```

(Insert under `letta-server.environment`, after `LITELLM_LOG: INFO`.)

- [ ] **Step 2: Add `speak_text` tool to agent via API**

```bash
TOOL_SOURCE_CODE='def speak_text(text: str, voice: str = "ru-RU-SvetlanaNeural") -> str:
    """Синтезировать речь через edge-tts sidecar. Возвращает JSON с base64 и timing."""
    import os
    import requests
    import json
    import time
    base_url = os.environ.get("TTS_BASE_URL", "http://tts:8000")
    t_start = time.monotonic()
    try:
        resp = requests.post(
            f"{base_url}/synthesize",
            json={"text": text, "voice": voice},
            timeout=60,
        )
        resp.raise_for_status()
        data = resp.json()
        t_end = time.monotonic()
        tool_ms = int((t_end - t_start) * 1000)
        return json.dumps({
            "audio_base64": data["audio_base64"],
            "filename": data["filename"],
            "duration_sec": data["duration_sec"],
            "synthesis_ms": data.get("synthesis_ms", tool_ms),
            "tool_total_ms": tool_ms,
        })
    except requests.exceptions.RequestException as e:
        return json.dumps({"error": f"TTS unavailable: {e}"})'

TOOL_JSON=$(python3 -c "
import json
tool = {
    'name': 'speak_text',
    'description': 'Озвучить текст через TTS (edge-tts). Используй когда Игорь просит голосом: слова \"скажи\", \"расскажи\", \"озвучь\", \"прочитай\", \"голосом\", \"voice\". Возвращает аудио-файл (.ogg), который будет отправлен как голосовое сообщение. БЕЗ отправки текста в этом случае.',
    'parameters': {
        'type': 'object',
        'properties': {
            'text': {'type': 'string', 'description': 'Текст для озвучки'},
            'voice': {'type': 'string', 'default': 'ru-RU-SvetlanaNeural', 'description': 'Голос TTS'}
        },
        'required': ['text']
    },
    'source_code': '''$TOOL_SOURCE_CODE'''
}
print(json.dumps({'tools': [tool]}))
")

# Create tool via letta API
curl -sS -X POST "http://localhost:8283/v1/tools/" \
    -H "Content-Type: application/json" \
    -d "$TOOL_JSON" | python3 -c "
import json,sys
d=json.load(sys.stdin)
if isinstance(d,list) and d: tool_id=d[0].get('id')
else: tool_id=d.get('id')
print('TOOL_ID:', tool_id)
" 2>&1 | tail -1

# Then attach to agent
TOOL_ID=$(curl -sS "http://localhost:8283/v1/tools/?name=speak_text" -H "Authorization: Bearer ${LETTA_API_KEY}" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d[0]['id'] if d else '')")
curl -sS -X PATCH "http://localhost:8283/v1/agents/agent-d622b194-88c6-4972-8421-fda92c1753a0/tools/attach/${TOOL_ID}" \
    -H "Content-Type: application/json" -d '{}' | head -c 200
echo
```

Expected: TOOL_ID printed, attach returns 200.

- [ ] **Step 3: PATCH persona to add TTS rules**

```bash
# Fetch current persona
curl -sS "http://localhost:8283/v1/agents/agent-d622b194-88c6-4972-8421-fda92c1753a0/core-memory" -o /tmp/persona.json
# Append new rules
python3 -c "
import json
d = json.load(open('/tmp/persona.json'))
for b in d.get('blocks', []):
    if b.get('label') == 'persona':
        v = b.get('value', '') or ''
        addition = '''
ВАЖНО: TTS (text-to-speech). Когда в сообщении Игоря есть слова «скажи», «расскажи», «озвучь», «прочитай», «голосом», «voice» (без учёта регистра) — отвечай ТОЛЬКО голосовым: вызови тулу speak_text(text=...) и отправь результат как voice message через MessageChannel с mediaPath. НЕ отправляй текст в этом случае. Иначе — обычный текстовый ответ как раньше.

В конце КАЖДОГО текстового (НЕ голосового) ответа добавляй блок (после пущей строки): [debug: llm=X.Xs] — приблизительная оценка времени моего LLM-ответа в секундах. Считай примерно, не точно. Для голосовых ответов debug-блок НЕ добавляй.'''
        new_v = v.rstrip() + '\n' + addition.strip() + '\n'
        b['value'] = new_v
json.dump(d, open('/tmp/persona_new.json', 'w'))
"
# PATCH
curl -sS -X PATCH "http://localhost:8283/v1/agents/agent-d622b194-88c6-4972-8421-fda92c1753a0/core-memory/blocks/persona" \
    -H "Content-Type: application/json" \
    -d @/tmp/persona_new.json | head -c 200
echo
```

Expected: 200, persona updated with new rules.

- [ ] **Step 4: Bring up tts service and restart letta-server (env update)**

```bash
cd /home/katya/letta
docker compose up -d tts 2>&1 | tail -3
docker compose restart letta-server 2>&1 | tail -3
sleep 25
docker compose ps 2>&1 | grep tts
# Verify health
curl -sS http://localhost:8283/v1/agents/agent-d622b194-88c6-4972-8421-fda92c1753a0 | python3 -c "
import json,sys
d=json.load(sys.stdin)
tools=[t['name'] for t in d.get('tools',[])]
print('  speak_text in tools:', 'speak_text' in tools)
"
# Verify tts sidecar reachable from letta-server
docker compose exec -T letta-server bash -c "curl -sS http://tts:8000/health" 2>&1 | head -1
```

Expected: `speak_text in tools: True`, tts health returns `{"status":"ok"}`.

- [ ] **Step 5: Manual e2e test — request TTS from user**

Ask the user to send to Telegram bot: "Маша, скажи привет" (Telegram is unblocked later in the day; for now WhatsApp or wait).

For WhatsApp, ask the user to send: "скажи привет"

Expected:
- Telegram/WhatsApp: voice message (no text) with Russian audio "Привет"
- Audio length ~1 sec

If WhatsApp not reachable now (user blocked), use a direct test via API:

```bash
# Direct test: send user message via API and check agent's outgoing
curl -sS -X POST "http://localhost:8283/v1/agents/agent-d622b194-88c6-4972-8421-fda92c1753a0/messages" \
    -H "Content-Type: application/json" -H "Authorization: Bearer ${LETTA_API_KEY}" \
    -d '[{"role":"user","content":"Маша, скажи привет тест"}]' 2>&1 | head -c 500
echo
# Wait for agent response
sleep 10
curl -sS "http://localhost:8283/v1/agents/agent-d622b194-88c6-4972-8421-fda92c1753a0/messages?limit=3" | python3 -c "
import json,sys
d=json.load(sys.stdin)
for m in d[:3]:
    mt=m.get('message_type'); date=m.get('date')
    tc=m.get('tool_call') or {}
    tr=m.get('tool_return') or {}
    txt=''
    if mt=='tool_call_message' and tc.get('name')=='speak_text':
        a=tc.get('arguments',{})
        if isinstance(a,str):
            try: a=json.loads(a)
            except: a={}
        txt=f'CALL speak_text(text={a.get(\"text\",\"\")[:60]!r})'
    elif mt=='tool_return_message':
        txt=f'RESULT: {str(tr)[:120]}'
    elif mt=='assistant_message':
        c=m.get('content')
        if isinstance(c,list):
            for p in c:
                if isinstance(p,dict) and p.get('type')=='text':
                    txt=p.get('text','')[:120]
    print(date,'|',mt,'|',txt)
" 2>&1 | head -10
```

Expected: tool_call_message with `speak_text` and tool_return with `audio_base64`.

- [ ] **Step 6: Commit docker-compose changes**

```bash
cd /home/katya/letta
git add docker-compose.yml
git commit -m "feat(tts): add tts sidecar service + TTS_BASE_URL for letta-server"
```

---

### Task 3: Optional — add audio attachment handling to MessageChannel (if needed)

**Files:**
- Modify: `/home/katya/letta/letta-code-channels/patches/` (new patch file)
- Modify: `/home/katya/letta/letta-code-channels/Dockerfile` (wire the new patch)

**Note:** This task only runs if Task 2 step 5 reveals that `MessageChannel` does not currently support sending audio attachments. The spec assumes `mediaPath` is supported — verify during Task 2.

- [ ] **Step 1: Inspect `MessageChannel` tool definition in the bundle**

```bash
docker compose exec -T letta-code-channels bash -c "grep -n 'mediaPath\\|attachmentId\\|audio\\|voice' /usr/local/lib/node_modules/@letta-ai/letta-code/letta.js" 2>&1 | head -20
```

Look for: does the tool accept a `mediaPath` or `attachment` parameter? Is there an existing path for sending voice notes?

- [ ] **Step 2: If gap found, add patch (otherwise skip this task)**

If `MessageChannel` does not natively support audio attachments for voice notes, add a patch `letta-code-channels/patches/patch-voice-attachment.py` that:
- Extends the `message_channel` function or `buildMessageChannelRequest` to accept an `audio_base64` parameter
- When provided, decodes the base64, writes to a temp file, sets `mediaPath` to the temp file
- Removes temp file after channel send

Then wire it in `Dockerfile` (similar to `patch-debug-tagging.py`).

If no gap found, mark this task as not needed and commit a doc note in the spec.

- [ ] **Step 3: Commit patch (if created)**

```bash
cd /home/katya/letta
git add letta-code-channels/patches/ letta-code-channels/Dockerfile
git commit -m "feat(voice): patch MessageChannel to support audio attachments"
```

---

## Self-Review

**Spec coverage:**
- Sidecar `/synthesize` endpoint with `text`, `voice` params, max 5000 chars → Task 1 (Step 2) ✓
- Default voice `ru-RU-SvetlanaNeural` → Task 1 (Step 2, line 19) ✓
- OGG/Opus format (we use MP3 from edge-tts, .ogg filename — could refine) → Task 1 (Step 2) — partial match, MP3 wrapped in .ogg name; consider ffmpeg conversion in fix round if needed
- Max 5000 chars truncation with warning → Task 1 (Step 2, line 80) ✓
- In-memory LRU cache 50 entries → Task 1 (Step 2) ✓
- `synthesis_ms` timing → Task 1 (Step 2) ✓
- `GET /health` → Task 1 (Step 2) ✓
- `GET /voices` → Task 1 (Step 2) ✓
- Docker compose service → Task 2 (Step 1) ✓
- Tool `speak_text` source code with timing → Task 2 (Step 2) ✓
- Tool без approval → not set in JSON (default) ✓
- Persona rule: trigger-слова → only voice → Task 2 (Step 3) ✓
- Persona rule: text + debug `[llm=X.Xs]` → Task 2 (Step 3) ✓
- Error: sidecar unavailable → tool returns error, agent fallback to text → Task 2 (Step 2, source_code) ✓
- Error: text > 5000 → sidecar truncates → Task 1 (Step 2) ✓
- End-to-end test: «Маша, скажи привет» → voice in WhatsApp → Task 2 (Step 5) ✓
- Cleanup path documented → spec section "Удаление / откат" — noted for human ✓
- "After TTS" — letta-2bx RAG fix → documented in spec ✓

**Placeholder scan:** No TBD/TODO. `OGG/Opus format` mentioned in spec — we use MP3 in code. Acceptable as start; if user wants true OGG/Opus, can add ffmpeg conversion in fix round.

**Type consistency:**
- `speak_text(text: str, voice: str = "ru-RU-SvetlanaNeural") -> str` — source_code matches JSON params ✓
- `SynthesizeResponse` Pydantic model has all fields used in /synthesize ✓
- Persona block PATCH reads current value, appends, writes back — consistent with knowledge-pipeline Task 4 pattern ✓

**Idempotency:**
- Tool creation: `POST /v1/tools/` may fail if tool exists — check by querying first, re-create only if needed
- Persona PATCH: appends, doesn't overwrite — safe
- Cache in sidecar: same text returns same response (consistent)
